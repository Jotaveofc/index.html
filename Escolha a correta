<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pergunta Divertida</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f0f0f0;
            text-align: center;
        }
        h1 {
            margin-bottom: 20px;
        }
        button {
            padding: 15px 30px;
            margin: 10px;
            font-size: 16px;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            transition: all 0.2s;
        }
        #botaoChato {
            background-color: #ff4d4d;
            color: white;
            position: absolute;
        }
        #botaoCorreto {
            background-color: #4CAF50;
            color: white;
        }
        #resposta {
            margin-top: 30px;
            font-size: 20px;
            color: green;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <h1>O que a gente vai fazer assim que eu chegar?</h1>

    <button id="botaoChato">Sair pra jantar</button>
    <button id="botaoCorreto">Me dar a bct</button>

    <div id="resposta"></div>

    <script>
        const botaoChato = document.getElementById('botaoChato');
        const botaoCorreto = document.getElementById('botaoCorreto');
        const resposta = document.getElementById('resposta');

        botaoChato.addEventListener('mouseover', () => {
            const maxX = window.innerWidth - botaoChato.offsetWidth;
            const maxY = window.innerHeight - botaoChato.offsetHeight;

            const randomX = Math.floor(Math.random() * maxX);
            const randomY = Math.floor(Math.random() * maxY);

            botaoChato.style.left = `${randomX}px`;
            botaoChato.style.top = `${randomY}px`;
        });

        botaoCorreto.addEventListener('click', () => {
            resposta.innerText = 'Resposta certa!';
        });
    </script>

</body>
</html>
